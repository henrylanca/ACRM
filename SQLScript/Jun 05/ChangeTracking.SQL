use MSDEV

ALTER DATABASE MSDEV
SET CHANGE_TRACKING= ON
(CHANGE_RETENTION= 2 DAYS, AUTO_CLEANUP = ON)

ALTER TABLE WASADMIN.CUSTOMER
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

ALTER TABLE WASADMIN.PERSONDETAILS
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

ALTER TABLE WASADMIN.CUSTOMERDOCDTL
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

ALTER TABLE WASADMIN.ADDRESS
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

ALTER TABLE WASADMIN.UBTB_CUSTEMPLOYMENTDETAILS
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

ALTER TABLE WASADMIN.UBTB_CUSTOMERCONNECTIONDETAILS
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

ALTER TABLE WASADMIN.ORGDETAILS
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

ALTER TABLE SCOTIA.SCOTIA_CORPORATEINFO
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

USE OP_DEV2

ALTER DATABASE OP_DEV2
SET CHANGE_TRACKING= ON
(CHANGE_RETENTION= 2 DAYS, AUTO_CLEANUP = ON)

ALTER TABLE dbo.CUST
ENABLE CHANGE_TRACKING
WITH (TRACK_COLUMNS_UPDATED=ON)

